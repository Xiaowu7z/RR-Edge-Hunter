# 使用与商标说明

RR Edge Hunter 是非官方第三方界面，与 Cloudflare, Inc.、`badafans/better-cloudflare-ip` 维护者不存在隶属、合作、赞助或背书关系。相关名称与商标归各自权利人所有。

测速由仓库固定的 `better-cloudflare-ip` 原版程序执行。该程序会访问第三方维护接口并产生真实下载流量；结果只代表当前设备、网络出口和时间，不构成质量保证。

Cloudflare DNS 同步只在用户填写自己的 Zone 与 API Token 后执行：手动模式必须完成预览与二次确认；自动模式必须在启动任务时明确勾选并再次授权每轮写入这一条记录。使用者负责保护 Token、确认记录用途并评估 DNS 变更影响。

仅应在自己拥有或获授权的网络、域名与 Cloudflare Zone 上使用，并遵守适用法律和服务条款。
